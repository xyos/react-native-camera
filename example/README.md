This is an example project showing how to setup and use react-native-camera.

How to run:
1. clone this repo

2. navigate to the `example` directory

3. run `npm install`

4. run `react-native run-android`
